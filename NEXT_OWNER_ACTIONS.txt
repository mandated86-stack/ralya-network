RALYA / RLYA - NEXT OWNER ACTION

The repository, Devnet protocol proof and Mainnet launch tooling are already prepared.
Do NOT create a token manually and do NOT use the old Devnet addresses for production.

ONE OWNER CHECKPOINT COMES NEXT:

1) On the owner's own computer, download the current `mandated86-stack/ralya-network` main branch.
   A normal GitHub `Code -> Download ZIP` extraction is supported; a git clone is NOT required.

   Run ONE of:
   Windows:     scripts/mainnet_program_deploy.ps1
   macOS/Linux: scripts/mainnet_program_deploy.sh

   The script itself will:
   - refuse CI/cloud production-key generation;
   - run the RALYA source/security audit first;
   - create THREE separate local identities:
       1. permanent RLYA Program ID keypair,
       2. dedicated upgrade-authority keypair,
       3. dedicated Mainnet deployment/fee-payer keypair;
   - keep all three private files outside the downloaded project/repository;
   - NEVER require importing a Phantom/Solflare seed into the Solana CLI;
   - ask for an offline backup of all three local JSON key files;
   - build with the tested Solana 3.1.10 / Anchor 1.0.2 toolchain;
   - configure only the dedicated payer as the CLI fee wallet for Mainnet deployment;
   - show the dedicated payer PUBLIC address, CURRENT Mainnet rent estimate and current balance;
   - stop safely if the payer needs funding, while preserving the same three local identities for retry;
   - deploy only after the owner explicitly types DEPLOY-RLYA-MAINNET;
   - download the deployed program again and require an exact SHA-256/byte match;
   - move upgrade authority away from the fee payer to the separate upgrade authority;
   - produce RALYA_MAINNET_PROGRAM_PUBLIC.txt containing PUBLIC evidence only.

   If the dedicated payer does not have enough real SOL, DO NOT confirm deployment.
   Fund only the PUBLIC payer address printed by the script, then rerun the SAME script.
   Never send the payer/program/upgrade JSON files to anyone.

2) After successful deployment, return ONLY RALYA_MAINNET_PROGRAM_PUBLIC.txt to ChatGPT.
   NEVER send any JSON key file, seed phrase, private key or wallet recovery phrase.

AFTER THAT PUBLIC RECORD IS VERIFIED:
- ChatGPT updates the public Program ID/site evidence while the presale master switch stays OFF.
- Initial token metadata is already independent of Netlify and is served from the public RALYA GitHub repository.
- The owner launch console creates exactly 839,000,000 RLYA, funds every published allocation,
  permanently removes mint authority, confirms freeze authority is absent, and atomically starts
  the 365-day founder lock while finishing with the sale PAUSED.
- The owner-funded 1 USDC Mainnet smoke check atomically performs resume + referral + purchase +
  pause, verifies the exact 1% referral accounting, and finishes PAUSED.
- Only after public verification is green is the website presale switch enabled and the sale resumed.

Until those owner-signed Mainnet transactions happen:
Mainnet program = NOT DEPLOYED
Production RLYA mint = NOT CREATED
Real-money public presale = DISABLED
